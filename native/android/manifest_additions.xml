<!-- Adições ao android/app/src/main/AndroidManifest.xml (dentro de <application>) -->
<!-- e permissões (fora de <application>, dentro de <manifest>). -->

<!-- Permissões -->
<uses-permission android:name="android.permission.INTERNET" />
<uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

<!-- Dentro de <application> -->
<service
    android:name=".RemoteControlService"
    android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
    android:exported="false">
    <intent-filter>
        <action android:name="android.accessibilityservice.AccessibilityService" />
    </intent-filter>
    <meta-data
        android:name="android.accessibilityservice"
        android:resource="@xml/accessibility_service_config" />
</service>

<!-- Em android/app/src/main/res/values/strings.xml adicionar: -->
<!-- <string name="accessibility_desc">Permite ao técnico Crypto.Invest orientar e tocar no ecrã durante o suporte.</string> -->

<!-- Em android/app/build.gradle (dependencies) adicionar: -->
<!-- implementation("com.squareup.okhttp3:okhttp:4.12.0") -->
